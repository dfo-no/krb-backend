# --- Prod ---
# Database configuration
%prod.quarkus.datasource.jdbc.url=${DB_JDBC_URL}/${DB_DATABASE}?sslmode=require
%prod.quarkus.datasource.db-kind=postgresql
%prod.quarkus.datasource.username=${DB_USER}
%prod.quarkus.datasource.password=${DB_PASSWORD}
%prod.quarkus.hibernate-orm.database.generation=${DB_GENERATION}
%prod.quarkus.hibernate-orm.sql-load-script=dummy-data.sql,functions.sql
# OIDC Configuration
%prod.quarkus.oidc.auth-server-url=https://krb-backend-auth.azurewebsites.net/realms/kravbank
%prod.quarkus.oidc.client-id=kravbank
%prod.quarkus.oidc.credentials.secret=${KC_CRED_SECRET}
%prod.quarkus.oidc.tls.verification=none
# --- Dev ---
# Database configuration
%dev.quarkus.hibernate-orm.database.generation=drop-and-create
%dev.quarkus.datasource.devservices.password=test
%dev.quarkus.datasource.devservices.username=test
#%dev.quarkus.datasource.jdbc.url=${DB_JDBC_URL_DEV}/${DB_DATABASE_DEV}?sslmode=require
#%dev.quarkus.datasource.db-kind=postgresql
#%dev.quarkus.datasource.username=${DB_USER}
#%dev.quarkus.datasource.password=${DB_PASSWORD}
#%dev.quarkus.hibernate-orm.database.generation=${DB_GENERATION_DEV}
#%dev.quarkus.hibernate-orm.sql-load-script=dummy-data.sql,functions.sql
# OIDC Configuration
%dev.quarkus.oidc.auth-server-url=https://krb-backend-auth.azurewebsites.net/realms/kravbank
%dev.quarkus.oidc.client-id=kravbank
%dev.quarkus.oidc.credentials.secret=${KC_CRED_SECRET}
%dev.quarkus.oidc.tls.verification=none
%dev.quarkus.log.min-level=DEBUG
%dev.quarkus.log.category."io.quarkus.oidc".level=DEBUG
%dev.kravbank.frontend.link=http://krb-webclient.azurewebsites.net/
# --- Test ---
# Database configuration
%test.quarkus.hibernate-orm.database.generation=drop-and-create
#%test.quarkus.hibernate-orm.sql-load-script=dummy-data.sql,functions.sql
# OIDC Configuration
%test.quarkus.oidc.client-id=quarkus-app
%test.quarkus.oidc.credentials.secret=secret
# --- All ---
# OIDC Configuration
kravbank.frontend.link=https://anskaffelser.no/kravbank/
#quarkus.hibernate-orm.log.sql=true
quarkus.hibernate-orm.database.generation.create-schemas=false
###LIQUIBASE#####
# Liquibase minimal config properties
quarkus.liquibase.migrate-at-start=true
# Liquibase optional config properties
quarkus.liquibase.change-log=db/changeLog.sql
# quarkus.liquibase.validate-on-migrate=true
# quarkus.liquibase.clean-at-start=false
# quarkus.liquibase.database-change-log-lock-table-name=DATABASECHANGELOGLOCK
# quarkus.liquibase.database-change-log-table-name=DATABASECHANGELOG
# quarkus.liquibase.contexts=Context1,Context2
# quarkus.liquibase.labels=Label1,Label2
# quarkus.liquibase.default-catalog-name=DefaultCatalog
# quarkus.liquibase.default-schema-name=DefaultSchema
# quarkus.liquibase.liquibase-catalog-name=liquibaseCatalog
# quarkus.liquibase.liquibase-schema-name=liquibaseSchema
# quarkus.liquibase.liquibase-tablespace-name=liquibaseSpace
